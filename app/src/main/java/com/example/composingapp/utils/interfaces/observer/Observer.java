package com.example.composingapp.utils.interfaces.observer;

public interface Observer {
    /**
     * Updates this Observer. Called when the Observable notifies its Observers of a change.
     */
    void update();
}
