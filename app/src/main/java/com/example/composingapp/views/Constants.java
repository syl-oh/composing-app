package com.example.composingapp.views;

public interface Constants {
     int TOTAL_LINES = 17;
     int TOTAL_SPACES = TOTAL_LINES - 1;
     int STEM_WIDTH = 3;
     float NOTE_W_TO_H_RATIO = (float) (4.0 / 3.0);
}
